package test;

/**
 * @Description:
 * @author: Anhlaidh
 * @date: 2020-08-18 16:26
 */
public class count {
    public static void main(String[] args) {
        System.out.println((1 / (2 * Math.PI)) * (1 + Math.pow(Integer.MAX_VALUE, 2)) + (1 / (2 * Math.PI)) * (1 + Math.pow(Integer.MIN_VALUE, 2)));
        System.out.println(Integer.MAX_VALUE/0);
        System.out.println(Integer.MIN_VALUE);
    }
}
